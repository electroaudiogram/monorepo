---
import { type Payload } from '@electroaudiogram/api'

import API from '../api'
import Layout from '../layouts/Layout.astro'

const data = await API.getCollection<Payload.Media>('media')

console.log(data)
---

<Layout metadata={{}}> Hello world </Layout>
